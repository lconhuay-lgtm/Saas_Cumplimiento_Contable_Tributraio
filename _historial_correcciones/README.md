# Historial de correcciones puntuales

Scripts `.bat`/`.py` de una sola vez (ya aplicados y mergeados al código actual)
más sus logs de ejecución (`resultado_*.log`). Se archivaron aquí en la Fase R3
del plan de remediación para despejar la raíz del repo — no se borraron porque
sirven como registro de qué se hizo y cuándo, pero **no están pensados para
volver a correrse desde esta carpeta** (varios asumen que `%~dp0` es la raíz
del proyecto, ya no lo es).

Herramientas que siguen vigentes y sí se usan hoy viven en `../scripts/`.
